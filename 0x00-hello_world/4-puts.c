#include <stdio.h>
/**
 * main - entry point
 *
 * description - this message prints a message to the standard output .
 * followed by a new line, it serves as the starting point of the program.
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle\n");
	return (0);
}
