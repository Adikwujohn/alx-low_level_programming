#include <stdio.h>
/**
 * main - entry point
 *
 * description: this function prints a message to standard out
 * followed by a newline it serves as the starting point of the program
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
